#program on search for an element in an array by calling function
input:

from array import *
arr=array('i',[])
n=int(input("enter length of the array: "))
for i in range(n):
    x=int(input("enter the element: "))
    arr.append(x)
print(arr)
s=int(input("enter element to search: "))
k=0
for e in arr:
    if e==s:
        break
    k+=1
print("index is",arr.index(s))

output:

enter length of the array: 4
enter the element: 12
enter the element: 14
enter the element: 16
enter the element: 18
array('i', [12, 14, 16, 18])
enter element to search: 18
index is 3
