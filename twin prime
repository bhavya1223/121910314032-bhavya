#program on twin prime
input:

n=int(input("enter limit:"))
prime = [True for i in range(n + 1)]
p = 2
while p * p <= n:
    if (prime[p] == True):
        for i in range(p * 2, n + 1, p):
            prime[i] = False
    p += 1
prime[0]= False
prime[1]= False
for i in range(2,len(prime)):
    if i<len(prime)-2:
        if prime[i]&prime[i+2]:
            print(i,i+2)
        if prime[i]&prime[i-2]:
            print(i,i-2)
            
output:

enter limit:10
3 5
5 7
5 3
7 5
